{
  "name": "yz-ad-club-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "vercel-build": "echo 'No build step required for API-only project'"
  }
  "dependencies": {
    "form-data": "^4.0.0",
    "node-fetch": "^2.6.7"
  },
  "scripts": {
    "vercel-build": "echo 'No build step required for API-only project'"
  }
}
